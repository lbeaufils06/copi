spring:
  datasource:
    url: jdbc:sqlite:copi.db
    driver-class-name: org.sqlite.JDBC

  jpa:
    database-platform: com.backend.copi.config.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  crypto:
    secret: ${MASTER_KEY}
  mysqldump:
    path: "C:/Program Files/MySQL/MySQL Server 8.0/bin/mysqldump.exe"

server:
  port: 8080
